{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd web && npm run build",
    "install-web": "cd web && npm install",
    "heroku-postbuild": "npm run install-web && npm run build",
    "web": "cd web && npm start",
    "start": "node ./api/app.js",
    "dev": "nodemon ./api/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}